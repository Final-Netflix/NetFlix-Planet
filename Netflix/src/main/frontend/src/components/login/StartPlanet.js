import React from 'react';
import LoginHeader from './LoginHeader';

const StartPlanet = () => {
    return (
        <div>
            <LoginHeader/>
        </div>
    );
};

export default StartPlanet;